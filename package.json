{
  "name": "explorex-ai-trip-planner",
  "version": "1.0.0",
  "private": true,
  "description": "EXPLOREX - Production-ready AI Trip Planner rebuilt for local development and Vercel deployment.",
  "scripts": {
    "dev": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:all": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start": "node server/server.js",
    "install:all": "npm install && npm install --prefix client && npm install --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
